/** @type {import('next').NextConfig} */
const nextConfig = {
  eslint: {
    // Warning: Disables ESLint. Only for development or testing
    ignoreDuringBuilds: true,
  },
};

module.exports = nextConfig;
